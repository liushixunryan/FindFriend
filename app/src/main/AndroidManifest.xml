<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ryan.findfriend">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".LoginActivity">


        </activity>
        <activity android:name=".Register.PerfectoneActivity"></activity>
        <activity android:name=".Register.PerfecttwoActivity" />
        <activity android:name=".Register.PerfectthreeActivity" />
        <activity android:name=".Register.PerfectfourActivity"></activity>
        <activity android:name=".Register.PerfectfiveActivity"></activity>
        <activity android:name=".Register.PerfectsixActivity" />
        <activity android:name=".Register.PerfectsevenActivity" />
        <activity android:name=".Register.CreatActivity"></activity>
        <activity android:name=".SouSuoActivity">


        </activity>
    </application>

</manifest>